const observeProcess = async (job) => {
    // checks some generic mongo queries and do actions:
    // 1- disable endpoints if something is wrong
    // 2- alert admin
    // 
    console.log(job.data)
}

export {
    observeProcess,
};
